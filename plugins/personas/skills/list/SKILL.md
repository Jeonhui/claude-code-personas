---
description: List all Claude Code profiles and highlight the currently active one
---

List all Claude Code profiles. Run:

```bash
node "${CLAUDE_PLUGIN_ROOT}/dist/index.js" list
```

Display the output to the user. If there are no profiles, suggest creating one with `/personas:create`.
